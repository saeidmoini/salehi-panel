project_name: salehi-panel
project_root: /opt/salehi-panel
repo_url: https://github.com/user/project.git
project_user: root
project_group: root

backend_dir: "{{ project_root }}/backend"
frontend_dir: "{{ project_root }}/frontend"
venv_path: "{{ project_root }}/venv"

gunicorn_bind: 127.0.0.1:8000
gunicorn_workers: 4
gunicorn_module: app.main:app
env_file: "{{ backend_dir }}/.env"
backend_framework: fastapi
django_manage_dir: "{{ backend_dir }}"
django_static_dir: "{{ backend_dir }}/staticfiles"

db_name: project_panel
db_user: project_user
db_pass: "CHANGE_ME"
db_host: localhost
db_port: 5432
database_url: ""   # leave empty to auto-build with URL-encoded password; or set explicitly

secret_key: "CHANGE_ME_SECRET"
dialer_token: "CHANGE_ME_DIALER_TOKEN"
google_sheet_webhook_url: ""
google_sheet_webhook_token: ""
google_sheet_webhook_timeout_seconds: 10
cors_origins:
  - "http://localhost:5173"
  - "http://127.0.0.1:5173"
  - "https://panel.example.com"

domain: panel.example.com
ssl_fullchain: "/etc/nginx/ssl/{{ domain }}/fullchain.crt"
ssl_privkey: "/etc/nginx/ssl/{{ domain }}/privkey.key"
ssl_dir: "/etc/nginx/ssl/{{ domain }}"
enable_ssl: true
arvan_token: ""   # set when issuing via Arvan; keep empty in repo
acme_sh_path: "/root/.acme.sh/acme.sh"
acme_email: "admin@example.com"   # used for ACME account registration
acme_reset_account: false        # set true if you need to wipe/rehydrate the acme account

nginx_http_port: 80
nginx_https_port: 443

node_command: npm
frontend_api_base: "https://{{ domain }}/api"

# Optional initial admin seed (leave empty if not needed)
initial_admin_user: ""
initial_admin_password: ""
